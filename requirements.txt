#note that cudatoolkit 11.6 requires conda-forge to also be installed
#when creating a new environment you can run the below:
#conda create  -n char_ext python=3.8 pytorch torchvision torchaudio numpy  cudatoolkit=11.6 -c pytorch -c conda-forge

pytorch = 1.12.0
numpy = 1.23.0
torchvision = 0.12.0
torchaudio = 0.13.0
cudatoolkit=11.6
